import { DelieveyDatePipe } from './delievey-date.pipe';

describe('DelieveyDatePipe', () => {
  it('create an instance', () => {
    const pipe = new DelieveyDatePipe();
    expect(pipe).toBeTruthy();
  });
});
